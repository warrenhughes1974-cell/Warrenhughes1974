# Data Sanitizer moved

All sanitizer files are in **`QLA_Data_Sanitizer/`**.

See [QLA_Data_Sanitizer/README.md](QLA_Data_Sanitizer/README.md).

```powershell
cd QLA_Data_Sanitizer
pip install -r requirements.txt
python sanitize_app.py
```

Supports **CSV and DBF** (character-field PII only).
